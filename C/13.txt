/* Program to merge lines alternately from two files and writes the results to new file */
ÿÿÿÿ#include <stdlib.h>
ÿÿÿÿ
ÿÿÿÿ    FILE *firstPtr, *secondPtr, *mergedPtr;
ÿÿÿÿ    /* Open the files */
ÿÿÿÿ    secondPtr = fopen("lower.c", "r");
ÿÿÿÿ    
ÿÿÿÿ    int i = 1;
ÿÿÿÿ        if( i % 2 == 0 || strlen(line1) == 0 )
ÿÿÿÿ        else if ( i % 2 != 0 || strlen(line2) == 0 )
ÿÿÿÿ        i++;
ÿÿÿÿ    
ÿÿÿÿ    fclose(secondPtr);
ÿÿÿÿ    
ÿÿÿÿ}
/* Program to copy the contents of one file to another after converting all the uppercase
}
#include <stdio.h>
}
#include <ctype.h>
}
    FILE *fromPtr, *toPtr;
}
    toPtr = fopen("copy", "w");
}
    if( fromPtr == NULL || toPtr == NULL ){
}
        exit(1);
}
    
}
    do {
}
        if( isupper(c) )
}
        else
}
    } while( c != EOF );
}
    fclose(fromPtr);
}
    return 0;
}
